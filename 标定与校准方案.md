# 标定与校准方案

## 1. 目标与范围

本方案用于统一相机与场地的标定与校准流程，确保进球判定、投篮点定位、区域划分与多机位一致性稳定可靠。

覆盖范围包括：

- 相机内参（焦距、主点、畸变）。
- 相机外参（姿态与位置）。
- 场地单应性（像素到场地平面坐标）。
- 篮筐与篮板几何（中心、半径、平面）。
- 标定数据的版本化与有效期管理。

## 2. 标定数据模型

建议以结构化方式保存标定数据，便于回溯与复用。

字段建议：

- `camera_id`: 相机唯一标识。
- `intrinsics`: 相机内参矩阵与畸变系数。
- `extrinsics`: 外参（旋转矩阵、平移向量）。
- `homography`: 场地单应性矩阵。
- `rim_center_px`: 篮筐中心像素坐标。
- `rim_radius_px`: 篮筐半径像素。
- `backboard_plane`: 篮板平面参数或四点坐标。
- `court_model`: 场地线关键点（含三分线与罚球线）。
- `valid_from` / `valid_to`: 标定有效期。
- `calibration_version`: 标定版本号。
- `notes`: 标定备注与采集环境。

### 2.1 JSON 示例模板

```json
{
	"camera_id": "cam_a_01",
	"intrinsics": {
		"fx": 1180.5,
		"fy": 1182.1,
		"cx": 960.0,
		"cy": 540.0,
		"distortion": [0.01, -0.03, 0.0, 0.0, 0.0]
	},
	"extrinsics": {
		"rotation": [[0.99, 0.02, 0.04], [-0.01, 0.99, -0.03], [-0.04, 0.03, 0.99]],
		"translation": [1.2, 3.8, 2.6]
	},
	"homography": [[0.0012, -0.0003, 0.12], [0.0004, 0.0011, 0.08], [0.0, 0.0, 1.0]],
	"rim_center_px": [1240, 320],
	"rim_radius_px": 46.0,
	"backboard_plane": {
		"points_px": [[1180, 220], [1300, 220], [1310, 360], [1170, 360]]
	},
	"court_model": {
		"three_point_arc": [[220, 920], [420, 680], [720, 600], [1200, 600], [1500, 680], [1700, 920]],
		"free_throw_line": [[720, 720], [1200, 720]]
	},
	"valid_from": "2026-05-25T00:00:00Z",
	"valid_to": "2026-08-25T00:00:00Z",
	"calibration_version": "v1.0.0",
	"notes": "indoor_court_a"
}
```

## 3. 关键阈值与派生参数

建议以篮筐半径为尺度基准，派生关键阈值：

- `rim_radius_px`: 篮筐半径像素，建议以 30 帧滑动窗口取中位数。
- `rim_contact_dist`: 0.08 到 0.15 * `rim_radius_px`，触框距离阈值。
- `backboard_zone_depth`: 0.12 到 0.25 * `rim_radius_px`，篮板判定区域深度。
- `velocity_jump`: 2.0 到 4.0 像素/帧，加速度突变阈值。
- `roll_min_frames`: 6 到 12 帧，滚筐停留最短帧数。
- `hand_ball_dist`: 0.6 到 1.0 * 手掌关键点置信区间，补篮触球判定。
- `release_gap_frames`: 2 到 5 帧，手球分离最小帧数。
- `entry_exit_gap_frames`: 3 到 8 帧，入筐到出筐的时间窗口。

## 4. 标定流程

### 4.1 相机内参标定

- 使用棋盘格或圆点板采集 15 到 25 张图像。
- 估计相机内参矩阵与畸变系数，保存到 `intrinsics`。

### 4.2 相机外参标定

- 通过场地已知点或标定板求解相机姿态。
- 生成 `extrinsics`，确保统一场地坐标系。

### 4.3 场地单应性标定

- 选取场地线关键点，计算 `homography`。
- 校验三分线与罚球线位置误差不超过 2% 到 5%。

建议补充场地平面标定细节：

- 选点要求：至少 4 个不共线点，优先使用底线角点、罚球线两端点、三分线与边线交点。
- 坐标系定义：以底线中点或篮筐投影点为原点，$X$ 轴沿边线方向，$Y$ 轴沿中线方向。
- 点位采集：在图像上逐点点击对应的场地线关键点，记录像素坐标。
- 真实坐标：依据场地标准尺寸（FIBA 或 NBA），构建对应的世界坐标点。
- 计算方法：用 DLT 或 RANSAC 求解 `homography`，并对离群点做剔除。
- 质量验收：关键线段重投影误差建议 <= 3-5 像素，线段长度误差 <= 2% 到 5%。
- 产出数据：更新 `homography` 与 `court_model`，用于投篮点与三分线判定。

若需要三维尺度一致性，可在已知内参的前提下分解 `homography` 求平面法向量与相机高度。

### 4.4 篮筐与篮板几何标定

- 估计 `rim_center_px` 与 `rim_radius_px`。
- 标定篮板四角点或平面参数，生成 `backboard_plane`。

### 4.5 场地平面与篮板联合标定

当需要更稳定的进球判定与投篮点定位时，建议将“场地平面标定 + 篮板标定”联合作为一次流程执行。

联合流程建议

1. 先完成场地平面标定
	- 选取场地线关键点，求解 `homography`。
	- 在场地坐标系中建立基准尺度。

2. 再完成篮板标定
	- 在图像中选取篮板四角点，得到 `backboard_plane`。
	- 通过篮板真实尺寸约束，估计篮板在场地坐标系中的大小与位置。

3. 进行一致性约束
	- 检查篮板与场地基准线的相对位置是否符合规则尺寸。
	- 验证篮筐中心投影在篮板矩形内的合理区域。

4. 产出与保存
	- 同时更新 `homography`、`backboard_plane`、`rim_center_px`、`rim_radius_px`。
	- 将联合标定作为同一 `calibration_version` 归档。

若已具备相机内参，可在联合流程中使用 PnP 求解篮板平面姿态，进一步提升三维一致性。

### 4.6 质量校验与版本管理

- 使用 10 到 20 个投篮片段验证进球时间点与区域划分。
- 保存 `calibration_version` 与有效期，支持回滚与追踪。

## 5. 校准频率建议

- 固定机位：每 1 到 3 个月复核一次，镜头变化需立刻重标。
- 移动机位：每次布置后重新标定。
- 高精度需求：每次训练前快速标定篮筐位置与半径。
- 环境变化：光照或场地布置变化明显时建议重新标定。

## 6. 使用与回滚

- 推理流程优先使用最新且有效期内的标定版本。
- 若检测到异常漂移，切换到上一个稳定版本并触发重新标定。
- 标定记录应与训练会话绑定，便于结果追溯。

## 7. 最小标定清单

若时间有限，可采用最小标定策略：

- 确定篮筐中心与半径像素。
- 计算场地单应性用于区域划分。
- 保存标定版本并记录有效期。

## 8. 附录：模板与交互

### 8.1 FIBA/NBA 标准场地关键点坐标模板

坐标系建议：以底线中点为原点 $(0, 0)$，$X$ 轴沿边线方向（右为正），$Y$ 轴向场地内侧（中线方向）。

参数模板

- `court_length`: FIBA 28.00，NBA 28.65（米）。
- `court_width`: FIBA 15.00，NBA 15.24（米）。
- `hoop_center_y`: FIBA 1.575，NBA 1.600（米，篮筐中心距底线）。
- `ft_line_y`: FIBA 5.80，NBA 5.79（米）。
- `ft_line_half_width`: 1.80（米）。
- `three_pt_radius`: FIBA 6.75，NBA 7.24（米）。
- `three_pt_corner_offset`: FIBA 0.90，NBA 0.91（米，三分线直段距边线）。

关键点坐标模板

- 四角点：$(-W/2, 0)$，$(W/2, 0)$，$(-W/2, L)$，$(W/2, L)$。
- 篮筐中心：$(0, hoop\_center\_y)$。
- 罚球线端点：$(-ft\_line\_half\_width, ft\_line\_y)$，$(ft\_line\_half\_width, ft\_line\_y)$。
- 三分线直段：$x = \pm(W/2 - three\_pt\_corner\_offset)$，$y \in [0, y_{corner}]$。
- 三分线弧：以篮筐中心为圆心、半径 $three\_pt\_radius$，弧段端点的 $y_{corner} = hoop\_center\_y + \sqrt{three\_pt\_radius^2 - x_{corner}^2}$。

注：以上为标准参考值，实际场地可能存在差异，建议以场地实测为准。

### 8.2 标定工具交互步骤与点位清单

交互步骤建议

1. 选择标定模式：场地平面或篮板平面。
2. 选择视频帧或实时画面并暂停。
3. 按顺序点选关键点，系统实时显示误差。
4. 保存并生成标定版本号。

场地平面点位清单（按顺序）

1. 底线左角
2. 底线右角
3. 中线左角
4. 中线右角
5. 罚球线左端点
6. 罚球线右端点
7. 三分线直段左端点
8. 三分线直段右端点

篮板平面点位清单（按顺序）

1. 篮板左上角
2. 篮板右上角
3. 篮板右下角
4. 篮板左下角
5. 篮筐中心（可选）

### 8.3 自动估计平面法向量与相机高度

若已知相机内参 $K$，可通过单应性矩阵 $H$ 估计场地平面的法向量与相机高度。

计算步骤概要

1. 归一化单应性：$\hat{H} = K^{-1} H$。
2. 取列向量：$\hat{H} = [h_1\; h_2\; h_3]$。
3. 计算尺度因子：$\lambda = 1 / ||h_1||$。
4. 估计旋转与平移：$r_1 = \lambda h_1$，$r_2 = \lambda h_2$，$t = \lambda h_3$。
5. 平面法向量：$n = r_1 \times r_2$。
6. 相机中心：$C = -R^T t$，相机高度为 $C_z$。

说明：上述推导假设场地平面为世界坐标 $z=0$，若坐标系不同需相应变换。

### 8.4 联合标定交互点位清单

联合标定建议一次完成场地平面与篮板标定，点位顺序如下：

1. 底线左角
2. 底线右角
3. 中线左角
4. 中线右角
5. 罚球线左端点
6. 罚球线右端点
7. 篮板左上角
8. 篮板右上角
9. 篮板右下角
10. 篮板左下角
11. 篮筐中心（可选）

### 8.5 一致性约束的数值规则模板

建议用以下规则做一致性校验，并允许小范围误差：

- 篮板尺寸：宽 1.80m，高 1.05m，允许误差 ±2cm。
- 篮筐高度：3.05m，允许误差 ±3cm。
- 篮筐到篮板平面距离：0.15m，允许误差 ±2cm。
- 篮板平面到场地底线距离：FIBA 1.20m，NBA 1.219m，允许误差 ±3cm。
- 篮筐中心距底线：FIBA 1.575m，NBA 1.600m，允许误差 ±3cm。

若出现超限，建议提示“标定异常”并要求复核点位。

### 8.6 联合标定 JSON 输出字段示例

```json
{
	"camera_id": "cam_a_01",
	"court_plane": {
		"homography": [[0.0012, -0.0003, 0.12], [0.0004, 0.0011, 0.08], [0.0, 0.0, 1.0]],
		"court_model": {
			"corners": [[-7.5, 0.0], [7.5, 0.0], [-7.5, 28.0], [7.5, 28.0]],
			"free_throw_line": [[-1.8, 5.8], [1.8, 5.8]]
		}
	},
	"backboard_plane": {
		"points_px": [[1180, 220], [1300, 220], [1310, 360], [1170, 360]],
		"size_m": [1.80, 1.05]
	},
	"rim": {
		"center_px": [1240, 320],
		"radius_px": 46.0,
		"height_m": 3.05
	},
	"quality": {
		"reproj_error_px": 3.2,
		"line_length_error_pct": 2.1,
		"consistency_pass": true
	},
	"calibration_version": "v1.1.0",
	"valid_from": "2026-05-25T00:00:00Z",
	"valid_to": "2026-08-25T00:00:00Z"
}
```
